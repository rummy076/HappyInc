import React from "react";
import Nav from "../components/nav";
import "./contact.css"; // Import the CSS file

const Contact = () => {
  return (
    <div>
      <Nav />
      <div className="page__header">Contact</div>
    </div>
  );
};

export default Contact;
